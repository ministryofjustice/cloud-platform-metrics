apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cp-metrics-app-service
spec:
  selector:
    matchLabels:
      app: cp-metrics-app 
  endpoints:
  - interval: 24h
    port: http